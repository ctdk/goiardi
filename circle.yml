test:
  pre:
    - gem install fpm
    - go get github.com/ctdk/gox
  post:
    - cd packaging && /bin/bash packaging.sh
general:
  artifacts:
    - packaging/artifacts
deployment:
  production:
    branch: releases
    commands:
      - cd packaging && /bin/bash deploy.sh
