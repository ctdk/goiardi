test:
  pre:
    - gem install fpm
    - apt-get install rpm
    - go get github.com/ctdk/gox
  post:
    - cd packaging && /bin/bash packaging.sh
general:
  artifacts:
    - packaging/artifacts
deployment:
  production:
    branch: releases
    commands:
      - cd packaging && /bin/bash deploy.sh
